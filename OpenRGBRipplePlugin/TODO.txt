**Yes — every first-party file in that plugin tree was written for this project.** None of them are copied OpenRGB or Artemis source files.

**I wrote (you can treat as original work)**

| Path | Role |
|---|---|
| `OpenRGBRipplePlugin.cpp` / `.h` / `.json` / `.qrc` / `.png` | Plugin entry, metadata, icon |
| `RippleWidget.cpp` / `.h` | Config panel |
| `RippleEngine.h` | Ripple math |
| `KeyboardHook.cpp` / `.h` | Windows key hook |
| `KeyMap.cpp` / `.h` | VK → OpenRGB LED names |
| `OpenRGBRipplePlugin.pro`, `CMakeLists.txt` | Build |
| `build-plugin.ps1`, `fetch-openrgb.ps1` | Scripts |
| `LICENSE`, `README.md` | Docs |
| `sdk-client\*` | Standalone exe (not the plugin) |

`LICENSE` already marks this plugin **GPL-2.0-or-later**, same as OpenRGB, so it can legally load as a plugin.

**I did not write (third-party — do not relicense as yours)**

These show up **on your disk** after the build script; they are not original plugin source:

| Path | Whose | Typical licence |
|---|---|---|
| `OpenRGB\` | [CalcProgrammer1 / OpenRGB](https://gitlab.com/CalcProgrammer1/OpenRGB) | GPL-2.0 |
| `.qt\` | The Qt Company (5.15.2) | LGPL-3 / GPL / commercial |
| `.venv\` | Python + aqtinstall | various |
| `build\`, `release\` | Your compile output + Qt `moc`/`rcc` generated files | derived from the above |

**What *is* borrowed (ideas / APIs, not their files)**

- **OpenRGB plugin API, LED names (`Key: A`), SDK packet layout** — from OpenRGB’s public headers/docs. Using that API is expected; linking against OpenRGB as a plugin is why this is GPL-2.
- **“Key Press” ring / fill / soft** — behaviour modelled on [Artemis 2](https://github.com/Artemis-RGB/Artemis/). Their code was **not** copied. If you ship this, credit Artemis as inspiration; do not call it an Artemis plugin.

**Practical licensing**

- Distribute **your** sources (the table above) as **GPL-2.0-or-later**.
- Do **not** zip up `OpenRGB\`, `.qt\`, or `.venv\` as if you own them.
- The built `OpenRGBRipplePlugin.dll` is a GPL plugin that loads into GPL OpenRGB and links Qt (LGPL is compatible if you dynamically link, which you do).

If you publish it, a short NOTICE like “Ripple effect inspired by Artemis 2 Key Press; OpenRGB plugin API © OpenRGB authors” is enough. I am not a lawyer; this is an authorship inventory, not legal advice.